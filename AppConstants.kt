package com.safetyinoffice.safety.util

interface AppConstants {
    companion object {
        val SESSION_TOKEN: String = "SESSION_TOKEN"
        val SUCCESS_STATUS_CODE: Int = 200
        val DEVICE_TOKEN = "DEVICE_TOKEN"
    }
}